apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - _folder.yaml
replacements:
  - source:
      kind: Namespace
      fieldPath: metadata.name
    targets:
      - select:
          kind: GrafanaFolder
        fieldPaths:
          - _metadata.name
  - source:
      kind: Namespace
      fieldPath: metadata.name
    targets:
      - select:
          kind: GrafanaFolder
        fieldPaths:
          - _spec.title
