apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: &repo grimmory
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: *repo
  policy:
    semver:
      range: '>=2.0.0'
