---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: readarr
spec:
  capacity:
    storage: 512Mi # Must match the size of the existing Longhorn volume
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: longhorn
  csi:
    driver: driver.longhorn.io
    fsType: ext4
    volumeHandle: readarr # This should be the actual name of your Longhorn volume
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: readarr
  labels:
    app.kubernetes.io/name: &name readarr
    app.kubernetes.io/instance: *name
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 512Mi
  storageClassName: longhorn
  volumeName: readarr
