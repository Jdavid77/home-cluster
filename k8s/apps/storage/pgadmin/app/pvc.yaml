apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pgadmin-config
  labels:
    app.kubernetes.io/name: &name pgamin
    app.kubernetes.io/instance: *name
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gb
  storageClassName: longhorn