# yaml-language-server: $schema=https://k8s-schemas-jnobrega.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app mosquitto
spec:
  interval: 15m
  timeout: 5m
  maxHistory: 2
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  values:
    controllers:
      mosquitto:
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          init-config:
            image:
              repository: public.ecr.aws/docker/library/eclipse-mosquitto
              tag: 2.0.22@sha256:54c90ecc78645241b6aa272b2a5ac8fc20b0eaf02cc4dd431c0cc8d2fd4447dd
              pullPolicy: IfNotPresent
            command: ["/bin/sh", "-c"]
            args:
              - |
                cp /tmp/secret/* /mosquitto/external_config/;
                chmod 600 /mosquitto/external_config/passwdfile.pwd;
                mosquitto_passwd -U /mosquitto/external_config/passwdfile.pwd;
        containers:
          app:
            image:
              repository: public.ecr.aws/docker/library/eclipse-mosquitto
              tag: 2.0.22-openssl@sha256:54c90ecc78645241b6aa272b2a5ac8fc20b0eaf02cc4dd431c0cc8d2fd4447dd
            resources:
              requests:
                cpu: 5m
              limits:
                memory: 16Mi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: {drop: ["ALL"]}
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
        fsGroup: 65534
        fsGroupChangePolicy: OnRootMismatch
    service:
      app:
        controller: *app
        ports:
          mqtts:
            port: &mqtts-port 8883
          mqtt:
            port: &mqtt-port 1883
    route:
      tcp:
        kind: TCPRoute
        parentRefs:
          - kind: Gateway
            name: internal
            namespace: network
            sectionName: tcp
        rules:
          - backendRefs:
              - name: *app
                port: *mqtt-port
      tls:
        kind: TLSRoute
        parentRefs:
          - kind: Gateway
            name: internal
            namespace: network
            sectionName: tls
        rules:
          - backendRefs:
              - name: *app
                port: *mqtts-port
    persistence:
      config-file:
        type: configMap
        name: mosquitto-config
        advancedMounts:
          mosquitto:
            app:
              - path: /mosquitto/config/mosquitto.conf
                subPath: mosquitto.conf
              - path: /mosquitto/external_config/aclfile.acl
                subPath: aclfile.acl
      secret-file:
        type: secret
        name: mosquitto-secret
        advancedMounts:
          mosquitto:
            init-config:
              - path: /tmp/secret
      certs:
        type: secret
        name: jnobrega.com-tls
        advancedMounts:
          mosquitto:
            app:
              - path: /mosquitto/certs
      external-config:
        type: emptyDir
        globalMounts:
          - path: /mosquitto/external_config
      logs:
        type: emptyDir
        advancedMounts:
          mosquitto:
            app:
              - path: /mosquitto/log
