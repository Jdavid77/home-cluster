# yaml-language-server: $schema=https://k8s-schemas-jnobrega.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app vaultwarden
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  values:
    controllers:
      vaultwarden:
        replicas: 1
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          init-db:
            image:
              repository: ghcr.io/home-operations/postgres-init
              tag: 18.3@sha256:6fa1f331cddd2eb0b6afa7b8d3685c864127a81ab01c3d9400bc3ff5263a51cf
              pullPolicy: IfNotPresent
            envFrom:
              - secretRef:
                  name: vaultwarden-secret
        containers:
          vaultwarden:
            image:
              repository: vaultwarden/server
              tag: 1.35.8-alpine@sha256:f5d0368ee8d29b62afbf9b8e34dcd074e3e22a2973a82f947d2c3260f9e2389a
            env:
              DOMAIN: "https://vaultwarden.jnobrega.com"
              TZ: Europe/Lisbon
              SIGNUPS_ALLOWED: "false"
              INVITATIONS_ALLOWED: "true"
              WEBSOCKET_ENABLED: "true"
              WEBSOCKET_ADDRESS: 0.0.0.0
              WEBSOCKET_PORT: 3012
              SMTP_HOST: mail.gmx.com
              SMTP_FROM_NAME: Vaultwarden
              SMTP_PORT: 587
              SMTP_SECURITY: "starttls"
              ADMIN_TOKEN: ${VAULTWARDEN_ADMIN_TOKEN_HASHED}
              TRASH_AUTO_DELETE_DAYS: 7
              PASSWORD_HINTS_ALLOWED: false
            envFrom:
              - secretRef:
                  name: vaultwarden-secret
            probes:
              liveness: &probes
                enabled: false
              readiness: *probes
              startup: *probes
            resources:
              requests:
                cpu: 10m
                memory: 100Mi
              limits:
                memory: 250Mi
    service:
      vaultwarden:
        controller: vaultwarden
        ports:
          http:
            port: &port 80
    route:
      main:
        parentRefs:
          - kind: Gateway
            name: internal
            namespace: network
            sectionName: websecure
        hostnames:
          - vaultwarden.jnobrega.com
        rules:
          - backendRefs:
              - name: *app
                port: *port
