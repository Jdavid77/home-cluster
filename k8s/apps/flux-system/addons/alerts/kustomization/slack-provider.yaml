apiVersion: notification.toolkit.fluxcd.io/v1beta2
kind: Provider
metadata:
  name: slack-bot-ks
  namespace: flux-system
spec:
  type: slack
  channel: kustomizations-products
  address: https://slack.com/api/chat.postMessage
  secretRef:
    name: slack-bot-token
