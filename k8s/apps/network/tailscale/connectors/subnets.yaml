---
apiVersion: tailscale.com/v1alpha1
kind: Connector
metadata:
  name: dns-cidrs
spec:
  hostname: dns-cidrs
  subnetRouter:
    advertiseRoutes:
      - ${LB_INTERNAL_IP}/32
      - ${PIHOLE_IP}/32